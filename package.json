{
  "name": "pyx",
  "version": "1.0.0",
  "description": "On the fly image transformations",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "create-bundle": "docker build -t pyx-lambda .",
    "copy-bundle": "docker create --name temp pyx-lambda && docker cp temp:/var/task ./dist && docker rm temp"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.1022.0",
    "i": "^0.3.7",
    "npm": "^11.12.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.1020.0",
    "@aws-sdk/client-s3": "^3.1020.0",
    "archiver": "^7.0.1",
    "dotenv": "^17.3.1"
  }
}
